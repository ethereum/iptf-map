{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ethsystems.org/schemas/use-case.json",
  "title": "EthSystems Use Case Frontmatter",
  "description": "Schema for use case document frontmatter in EthSystems Map",
  "type": "object",
  "required": ["title"],
  "properties": {
    "title": {
      "type": "string",
      "description": "Use case title"
    },
    "primary_domain": {
      "type": "string",
      "enum": [
        "Payments",
        "Trading",
        "Custody",
        "Funds & Assets",
        "Identity & Compliance",
        "Data & Oracles",
        "Civic Coordination & Governance",
        "Post-Quantum Threats"
      ],
      "description": "Primary business domain (matches domains/ taxonomy)"
    },
    "secondary_domain": {
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } },
        { "type": "null" }
      ],
      "description": "Secondary business domain(s); optional"
    },
    "status": {
      "type": "string",
      "enum": ["draft", "ready"],
      "description": "Document status"
    },
    "priority": {
      "type": "string",
      "enum": ["high", "medium", "low"],
      "description": "Business priority"
    }
  },
  "additionalProperties": false
}
